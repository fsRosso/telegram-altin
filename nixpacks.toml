[phases.setup]
nixPkgs = ["python39", "python39Packages.pip", "chromium", "wget", "unzip"]

[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "pip install -r requirements.txt",
  "playwright install chromium"
]

[phases.build]
cmds = ["echo 'Build completed'"]
